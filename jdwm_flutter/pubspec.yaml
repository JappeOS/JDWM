name: jdwm_flutter
description: The Flutter UI library for the JDWM window manager.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.5.4

dependencies:
  flutter:
    sdk: flutter

  shade_ui:
    git:
      url: https://github.com/JappeOS/shade_ui.git
      ref: master

  prioritized_event:
    git:
      url: https://github.com/JappeOS/prioritized_event.git
      ref: main

  vector_math: ^2.1.4
  event: ^3.1.0

dev_dependencies:
  #flutter_test:
  #  sdk: flutter

  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true